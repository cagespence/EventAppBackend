query ($eventId: ID!) {
    event(id: $eventId) {
        event {
            id
            title
            description
            attendees {
                id
            }
        }
    }
}